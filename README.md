该代码一共有三版，循序渐进来理解梯度下降法拟合函数的过程并最终对比全量梯度下降和小量梯度下降在拟合函数时的异同，对比结果以表格和图像的方式表现出来：
第一版：先表示(x-3)²，再求其导数，然后用梯度下降法从0开始迭代若干步观察是否趋近于3，最后绘图
第二版：先表示(x-3)²+(y-2)²，再求其导数，然后用梯度下降法从(0,0)^T开始迭代若干步观察是否趋近于(3,2)^T，最后绘图
第三版：分别表示全量梯度下降和小量梯度下降，并对比它们在线性回归中的表现，最后将结果以表格和图像的方式表示出来。
